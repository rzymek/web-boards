Session.setDefault('gameInfo', {
    pieces: [],
    board: {
        image: '',
        width: 1,
        height: 1,
        grid: null
    }
});

Session.setDefault('selectedGame', null);

sprites = null;
