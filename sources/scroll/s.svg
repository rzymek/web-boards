<svg xmlns="http://www.w3.org/2000/svg"
     xmlns:xlink="http://www.w3.org/1999/xlink"
     xmlns:html="http://www.w3.org/1999/xhtml"
     width="4000px" height="3000px">

  <script type="text/ecmascript"><![CDATA[
    // from http://www.howtocreate.co.uk/tutorials/javascript/browserwindow
    function getScrollXY() {
      var scrOfX = 0, scrOfY = 0;
      if( typeof( window.pageYOffset ) == 'number' ) {
        //Netscape compliant
        scrOfY = window.pageYOffset;
        scrOfX = window.pageXOffset;
      } else if( document.body && ( document.body.scrollLeft || document.body.scrollTop ) ) {
        //DOM compliant
        scrOfY = document.body.scrollTop;
        scrOfX = document.body.scrollLeft;
      } else if( document.documentElement && ( document.documentElement.scrollLeft || document.documentElement.scrollTop ) ) {
        //IE6 standards compliant mode
        scrOfY = document.documentElement.scrollTop;
        scrOfX = document.documentElement.scrollLeft;
      }
      return [ scrOfX, scrOfY ];
    }
      
function handle(evt){
alert('y');
}
    function moveFixed(evt)
    {
      var scrollpos = getScrollXY();
      var fixed = document.getElementById("fixed");
      var tfm = fixed.transform.baseVal.getItem(0);
      tfm.setTranslate(scrollpos[0], scrollpos[1]);	
    }
    
    window.onscroll = moveFixed;
	 window.addEventListener('touchmove', handle, false);

  ]]></script>
  
  <circle cx="25%" cy="25" r="200" fill="cornflowerblue" stroke="blue" stroke-width="3" />
  <circle cx="50%" cy="2000" r="100" fill="green" stroke="green" stroke-width="3"  />
  
  <g id="fixed" transform="translate(0 0)">
    <rect width="100%" height="20" fill-opacity="0.5"/>
    <text y="1em" x="50" fill="white">This is a fixed svg header group</text>
  </g>
  
</svg>
